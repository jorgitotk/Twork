<% unless task.comments.blank? %>
	<ul>
	  <% task.comments.each do |comment| %>
	  		<i class="style-owner-comment"><%= comment.user.name %></i>
	  		</br>
	  		<%= comment.body %> 
	  		</br>
	  		</br>
	  <% end %>
	</ul>
<% else %>
  No comments has been registered.
<% end %>
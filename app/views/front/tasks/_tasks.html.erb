<% unless tasks.blank? %>
  <% tasks.each do |t| %>
    <%= render 'admin/general_view/modal_detail_task', task: t %>
  	<%= link_to t.name, '#', data: {toggle:"modal", target:"#modal-detail-task-#{t.id}"} %>
    <br>
  <% end %>
<% else %>
	No task has been found.
<% end %>
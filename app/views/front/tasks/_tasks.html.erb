<% unless tasks.blank? %>
  <% tasks.each do |task| %>
    <%= render 'admin/general_view/modal_detail_task', task: task %>
  	<%= link_to task.name, '#', data: { toggle:"modal", target:"#modal-detail-task-#{task.id}" } %>
    <br>
  <% end %>
<% else %>
	No task has been found.
<% end %>
<% if group.user_has_groups.blank? %>
  <div class="members">
    <br>
    No members has been found.
  </div>
<% else %>
  <div class="members">
    <table>
      <h4><b>Members:</b></h4>
      <% group.user_has_groups.each do |user_group| %>
        <tr>
          <td><%= user_group.user.name %></td>
          <td><%= link_to "<i class='fa fa-times'></i>".html_safe, {action: "destroy_member", member_id: user_group.user_id, group_id: group}, id:"delete-member-button", class:"btn btn-danger", data: { confirm: "#{user_group.show_member} will be removed of #{group.name}. Are you sure?" } , method: :delete, remote: true %></td>
          <td class="padding-td">.</td>
        </tr>
      <% end %>
    </table>
  </div>
<% end %>

<script type="text/javascript">
  $(document).ready(function(){
    $("#search_typeahead").val(''); 
  });
</script>